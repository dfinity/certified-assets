[package]
name = "plugin"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
assets-sync = { path = "../assets-sync" }
candid.workspace = true
serde.workspace = true
wit-bindgen = { workspace = true, features = ["realloc"] }
