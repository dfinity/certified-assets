# Demo a password protected asset canister

Open the page at:

http://frontend.local.localhost:8000

You will see an unauthorized page.

Open the page at:

http://frontend.local.localhost:8000?_auth_token=password

The cookie will get set and the page will reload
