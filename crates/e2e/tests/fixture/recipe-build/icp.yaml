networks:
  - name: local
    mode: managed
    gateway:
      port: 0

canisters:
  - name: frontend
    recipe:
      type: "file://recipe.hbs"
      configuration:
        dir: dist
        build:
          - sh -c 'echo "generated by build step" > dist/generated.txt'
