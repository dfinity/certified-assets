networks:
  - name: local
    mode: managed
    gateway:
      port: 0

canisters:
  - name: frontend
    build:
      steps:
        - type: pre-built
          path: ../../dist/canister.wasm

    sync:
      steps:
        - type: plugin
          path: ../../dist/plugin.wasm
          dirs:
            - dist-a
            - dist-b
